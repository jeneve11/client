import React from 'react';
import { StyleSheet, Text, View } from 'react-native';


export default class HashTag extends React.Component {
  render () {
    return (
        <View>
            <Text>HashTag.tsx</Text>
        </View>
    )
  }
}